[project]
name = "lalalangchain"
version = "0.1.0"
description = "Multimodal input — sending text and an image to a vision model via Ollama"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.1.0",
]
